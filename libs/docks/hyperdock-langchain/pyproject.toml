[project]
name = "hyperdock-langchain"
version = "0.3.4"
description = "hyperdock-langchain"
authors = [{ name = "Hyperpocket Team", email = "hyperpocket@vessl.ai" }]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "langchain>=0.3.15",
    "multiprocess>=0.70.17",
]

[tool.ruff]
extend = "../../../.ruff.toml"
