[project]
name = "hyperdock-container"
version = "0.3.7"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "hyperpocket",
    "pydantic>=2.10.2",
    "docker>=7.1.0",
]

[tool.uv.sources]
hyperpocket = { path = "../../hyperpocket", editable = true }

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]
